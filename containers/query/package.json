{
  "name": "query",
  "version": "1.0.0",
  "description": "Service to process query messages",
  "main": "query-service.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "aws-sdk": "^2.696.0"
  }
}
